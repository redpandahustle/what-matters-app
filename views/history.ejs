<!DOCTYPE html>
<html>
<head>
    <title>History</title>
    <link rel="stylesheet" type="text/css" href="/css/styles.css">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            font-family: Arial, sans-serif;
        }
        header {
            text-align: center;
        }
        nav {
            margin: 20px 0;
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
        }
        nav a {
            margin: 5px;
            text-decoration: none;
            color: #fff;
            background-color: #333;
            padding: 10px 15px;
            border-radius: 5px;
            display: inline-block;
        }
        nav a:hover {
            background-color: #555;
        }
        main {
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 20px;
        }
        .goal-history {
            width: 100%;
            max-width: 600px;
            margin-bottom: 20px;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
        }
        .goal-history h3 {
            text-align: center;
        }
        .goal-history ul {
            list-style-type: none;
            padding: 0;
        }
        .goal-history li {
            margin: 5px 0;
            font-size: 16px;
        }
        @media (max-width: 768px) {
            .goal-history {
                width: 90%;
            }
            nav {
                flex-direction: column;
            }
            nav a {
                margin: 5px 0;
            }
        }
        @media (max-width: 480px) {
            .goal-history li {
                font-size: 14px;
            }
            nav a {
                padding: 8px 10px;
                font-size: 14px;
                margin: 3px 0;
            }
        }
    </style>
</head>
<body>
    <header>
        <h1>History</h1>
        <nav>
            <a href="/what-matters">What Matters?</a>
            <a href="/today">Today</a>
            <a href="/history">History</a>
        </nav>
    </header>
    <main>
        <% goals.forEach(goal => { %>
            <div class="goal-history">
                <h3><%= goal.what_matters %></h3>
                <p><%= goal.why_it_matters %></p>
                <ul>
                    <% 
                    const sortedDates = Object.keys(entries).sort((a, b) => new Date(b) - new Date(a));
                    sortedDates.forEach(date => { 
                        if (entries[date][goal.id]) { 
                    %>
                            <li><%= date %>: <%= entries[date][goal.id].entry %></li>
                    <% 
                        } 
                    }) 
                    %>
                </ul>
            </div>
        <% }) %>
        <h2>Archived Goals</h2>
        <% archivedGoals.forEach(goal => { %>
            <div class="goal-history">
                <h3><%= goal.what_matters %></h3>
                <p><%= goal.why_it_matters %></p>
                <ul>
                    <% 
                    const sortedDates = Object.keys(entries).sort((a, b) => new Date(b) - new Date(a));
                    sortedDates.forEach(date => { 
                        if (entries[date][goal.id]) { 
                    %>
                            <li><%= date %>: <%= entries[date][goal.id].entry %></li>
                    <% 
                        } 
                    }) 
                    %>
                </ul>
            </div>
        <% }) %>
    </main>
</body>
</html>
